{% extends 'base.html.twig' %}

{% block title %}Langage{% endblock %}

{% block body %}
    <h1>Langage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ langage.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ langage.label }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ langage.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_langage_index') }}">back to list</a>

    <a href="{{ path('app_langage_edit', {'id': langage.id}) }}">edit</a>

    {{ include('langage/_delete_form.html.twig') }}
{% endblock %}
